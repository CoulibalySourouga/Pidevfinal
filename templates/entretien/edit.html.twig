{% extends 'base.html.twig' %}
{% block title %}update page{% endblock %}
{% block body %}
    <h1>Editer un entretien</h1>
    {{ form_start(formEntretien) }}
    {{ include('entretien/form.html.twig') }}
    <button class="btn btn-primary">Editer</button>
    {{ form_end(formEntretien) }}
{% endblock %}
